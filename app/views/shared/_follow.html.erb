<%= form_for Follow.new, url: follows_path, method: :post do |f| %>
              <%= f.hidden_field :followed_id, value: data.id %>
              <%= f.submit "Follow", class: "btn btn-info btn-sm"%>
            <% end %>
