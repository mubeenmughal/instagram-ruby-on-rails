<%= form_for Follow.new, url: follow_path(data.id), method: :delete do |f|%>
              <%= f.hidden_field :followed_id, value: data.id %>
              <%= f.submit "Unfollow", class: "btn btn-danger btn-sm" %>
            <% end %>
            
