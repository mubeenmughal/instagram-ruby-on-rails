
<%= form_tag unfollow_account_path(data.id), method: :post,remote: true, class: "disabled" do%>
  <%= hidden_field_tag :follow_id, data.id %>
  <%= submit_tag "Unfollow", class: "btn btn-danger btn-sm" %>

<% end %>
